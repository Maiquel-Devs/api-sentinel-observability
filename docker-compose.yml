services:
  paymentflow-api:
    container_name: paymentflow-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5086:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DOTNET_SYSTEM_NET_HTTP_USESOCKETSHTTPHANDLER=0
    restart: unless-stopped